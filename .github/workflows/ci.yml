name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
  # Refresh advisory data even when the repository is otherwise idle.
  schedule:
    - cron: '17 3 * * 0'

jobs:
  ci:
    name: cargo check / test / clippy
    runs-on: self-hosted
    env:
      CARGO_HOME: ${{ github.workspace }}/.ci/cargo-home-${{ github.run_id }}-${{ github.run_attempt }}-ci
      RUSTUP_HOME: ${{ github.workspace }}/.ci/rustup-home-${{ github.run_id }}-${{ github.run_attempt }}-ci
    steps:
      - name: Attach retained self-hosted checkout commit
        run: |
          if git -C "$GITHUB_WORKSPACE" rev-parse --is-inside-work-tree >/dev/null 2>&1; then
            git -C "$GITHUB_WORKSPACE" update-ref refs/clavenar/runner-retained HEAD
          fi

      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Prepare isolated Rust setup home
        run: install -d -m 0700 "$RUNNER_TEMP/home-$GITHUB_RUN_ID-$GITHUB_RUN_ATTEMPT-$GITHUB_JOB"

      - uses: dtolnay/rust-toolchain@4be7066ada62dd38de10e7b70166bc74ed198c30 # stable
        env:
          HOME: ${{ runner.temp }}/home-${{ github.run_id }}-${{ github.run_attempt }}-${{ github.job }}
        with:
          toolchain: 1.97.0
          components: rustfmt, clippy

      - name: cargo fmt
        run: cargo fmt --all -- --check

      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32 # v2

      - name: cargo check
        run: cargo check --all-targets

      - name: cargo test
        run: cargo test

      - name: cargo clippy
        run: cargo clippy --all-targets -- -D warnings

  # Supply-chain gate. cargo-deny runs four checks against `deny.toml` at
  # the repo root: advisories (RUSTSEC vulns + unmaintained), licenses
  # (allow-list), bans (multiple-version warnings), sources (only
  # crates.io). The deny.toml file is synced verbatim across all 14 Rust
  # repos — edit clavenar-specs first if policy changes, then mirror into
  # every other repo.
  supply-chain:
    name: cargo-deny + cyclonedx SBOM
    # The dependency graph is deterministic for a pinned Cargo.lock. Audit it
    # on review, on demand, and weekly as the advisory database changes.
    if: github.event_name == 'pull_request' || github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: self-hosted
    env:
      CARGO_HOME: ${{ github.workspace }}/.ci/cargo-home-${{ github.run_id }}-${{ github.run_attempt }}-supply-chain
      RUSTUP_HOME: ${{ github.workspace }}/.ci/rustup-home-${{ github.run_id }}-${{ github.run_attempt }}-supply-chain
    steps:
      - name: Attach retained self-hosted checkout commit
        run: |
          if git -C "$GITHUB_WORKSPACE" rev-parse --is-inside-work-tree >/dev/null 2>&1; then
            git -C "$GITHUB_WORKSPACE" update-ref refs/clavenar/runner-retained HEAD
          fi

      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      # Run both tools host-side. The dockerized cargo-deny action does not
      # inherit the runner's Rust toolchain consistently on self-hosted jobs.
      - name: Prepare isolated Rust setup home
        run: install -d -m 0700 "$RUNNER_TEMP/home-$GITHUB_RUN_ID-$GITHUB_RUN_ATTEMPT-$GITHUB_JOB"

      - uses: dtolnay/rust-toolchain@4be7066ada62dd38de10e7b70166bc74ed198c30 # stable
        env:
          HOME: ${{ runner.temp }}/home-${{ github.run_id }}-${{ github.run_attempt }}-${{ github.job }}
        with:
          toolchain: 1.97.0

      - uses: cargo-bins/cargo-binstall@ead08b90bd7b2e6d81963fb9cf0b7239f66d5db4 # main

      - name: install cargo-deny + cargo-cyclonedx
        run: |
          # Runner services share their default Cargo home. Force materializes
          # the pinned tools even if another job left stale install receipts.
          cargo binstall --no-confirm --force cargo-deny@0.20.2 cargo-cyclonedx@0.5.9
          test -x "$CARGO_HOME/bin/cargo-deny"
          test -x "$CARGO_HOME/bin/cargo-cyclonedx"
          echo "$CARGO_HOME/bin" >> "$GITHUB_PATH"

      - name: cargo-deny check all
        run: cargo deny check all

      - name: generate SBOM
        run: cargo cyclonedx --format json --describe crate

      - name: upload SBOM
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: sbom-${{ github.event.repository.name }}-${{ github.sha }}
          path: '*.cdx.json'
          if-no-files-found: error
          retention-days: 90
